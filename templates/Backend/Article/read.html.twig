{% extends 'base.html.twig' %}

{% block title %}
	{{ article.title }}
	{{ parent() }}
{% endblock %}

{% block body %}
	<section class="container mt-2">
		<h1>{{ article.title }}</h1>
		<p>{{ article.content }}</p>
	</section>
{% endblock %}
